const $ = s => document.querySelector(s);
let data;
try { data = await fetch('./data/stores.json').then(r => { if(!r.ok) throw new Error(); return r.json(); }); }
catch { $('#rows').innerHTML='<tr><td colspan="6">No synchronized data yet. Run <code>npm run sync</code>, then reload.</td></tr>'; throw new Error('No data'); }

$('#scope').textContent = `${data.center?.formattedAddress || 'Clearwater, Florida'} · ${data.radiusMiles}-mile radius`;
const stores = data.stores || [];
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function pct(v,t){return t?Math.min(100,Math.round(v/t*100)):100}
function renderSummary(rows){const counts={Legendary:0,Standard:0,Welcome:0};rows.forEach(s=>counts[s.tier.tier]++);$('#summary').innerHTML=`<div class="card">Stores<b>${rows.length}</b></div><div class="card">Legendary<b>${counts.Legendary}</b></div><div class="card">Standard<b>${counts.Standard}</b></div><div class="card">Welcome<b>${counts.Welcome}</b></div>`}
function render(){const q=$('#search').value.toLowerCase(), tier=$('#tier').value;const rows=stores.filter(s=>(!q||s.name.toLowerCase().includes(q))&&(!tier||s.tier.tier===tier));renderSummary(rows);$('#rows').innerHTML=rows.map((s,i)=>`<tr data-id="${esc(s.storeId)}"><td><strong>${esc(s.name)}${s.tier.isNew?'*':''}</strong><div class="fine">${esc(s.address)}</div></td><td class="tier ${s.tier.tier}">${s.tier.tier}${s.tier.isNew?'*':''}</td><td>${s.metrics.events}</td><td>${s.metrics.uniquePlayers}</td><td>${s.metrics.tickets}</td><td>${esc(s.tier.path)}</td></tr>`).join('')||'<tr><td colspan="6">No matching stores.</td></tr>';document.querySelectorAll('tbody tr[data-id]').forEach(tr=>tr.onclick=()=>show(stores.find(s=>String(s.storeId)===tr.dataset.id)));}
function show(s){const target=s.tier.nextTier==='Standard'?s.tier.standardTarget:s.tier.nextTier==='Legendary'?s.tier.legendaryTarget:null;const metric=(label,v,t)=>`<div class="metric"><div class="metric-line"><span>${label}</span><strong>${v} / ${t}</strong></div><div class="meter"><span style="width:${pct(v,t)}%"></span></div></div>`;$('#details').classList.remove('hidden');$('#details').innerHTML=`<h2>${esc(s.name)}${s.tier.isNew?'*':''}</h2><p><strong>Estimated tier: <span class="${s.tier.tier}">${s.tier.tier}</span></strong></p><p>${s.tier.nextTier?`To reach <strong>${s.tier.nextTier}</strong>: ${esc(s.tier.path)}`:'Highest published tier reached.'}</p>${target?metric('Events',s.metrics.events,target.events)+metric('Unique players',s.metrics.uniquePlayers,target.uniquePlayers)+metric('Event tickets',s.metrics.tickets,target.tickets):''}<p class="fine">First recorded Play Hub activity: ${s.firstActivity?new Date(s.firstActivity).toLocaleDateString():'Unknown'}${s.tier.isNew?` · Estimated first-year proration ${(s.tier.prorationFactor*100).toFixed(0)}%`:''}</p><p class="fine">Registration fetch failures: ${s.dataQuality.registrationFailures}. Prerelease eligibility exactness: ${s.dataQuality.prereleaseEligibilityKnown?'known':'not yet known'}.</p>`;$('#details').scrollIntoView({behavior:'smooth',block:'nearest'});}
$('#search').oninput=render;$('#tier').onchange=render;render();$('#footer').textContent=`Data generated ${new Date(data.generatedAt).toLocaleString()}. * Less than one year of recorded Play Hub activity; prorated thresholds are estimates, not official Ravensburger determinations.`;
